---
const suffix = '/work/';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Redirecting…</title>
    <meta name="robots" content="noindex" />
  </head>
  <body>
    <noscript>
      <p><a href="/en/work/">Continue in English</a></p>
      <p><a href="/es/work/">Continuar en español</a></p>
    </noscript>

    <script is:inline>
      const supportedLocales = ['en', 'es'];
      const preferredLocale = window.localStorage.getItem('preferred-locale');
      const browserLocales = (navigator.languages || [navigator.language])
        .map((locale) => locale.toLowerCase())
        .map((locale) => locale.split('-')[0]);

      const browserLocale = browserLocales.find((locale) => supportedLocales.includes(locale));
      const locale = supportedLocales.includes(preferredLocale) ? preferredLocale : browserLocale || 'en';
      window.location.replace(`/${locale}${'/work/'}`);
    </script>
  </body>
</html>
